<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>Info</title>
    <link rel="stylesheet" href="/static/stylesheets/style.css">
    <style>
        .container {
            /* max-width: 1080px; */
            max-width: 100vw;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;

        }

        .title {
            font-size: 32px;
            color: #f00;
        }

        .card {
            display: flex;
            flex-direction: column;
            width: 360px;
            margin: 20px;
            padding: 10px 40px;
            /* border: 2px solid #f00; */
            background-color: #fff;
            box-shadow: rgba(50, 50, 93, 0.25) 0px 13px 27px -5px, rgba(0, 0, 0, 0.3) 0px 8px 16px -8px;
        }

        .card button {
            background-color: #f00;
            border: none;
            padding: 10px 20px;
        }

        .card>button a {
            font-size: 18px;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
        }

        img {
            width: 240px;
        }
    </style>
</head>

<body>
    <h1 class="title">Info</h1>
    <div class="container">

        <% vidInfo.forEach(vid => { %>
        <div class="card">
            <div class="info">
                <h1 class="itag"> <%= title %> </h1>
                <img src="<%= thumbnail %> " alt="">
                <p class="quality"><%= vid.quality %> -> <%= vid.qualityLabel %> </p>
                <p><%= vid.itag %> </p>
            </div>
            <button><a href="<%= vid.url %> ">Download</a></button>
        </div>
        <% }) %>
    </div>


</body>

</html>